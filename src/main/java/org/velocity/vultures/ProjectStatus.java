package org.velocity.vultures;

public enum ProjectStatus {
    OPEN,      // Available for applications
    FULL,      // Application limit reached
    ARCHIVED   // No longer offered, but kept for history
}
